# G7 report — 2026-08-12
Gate: the language's refusals are falsifiable ("smart XFAILs"). RESULT: PASS.
Signal: maintainer directive — "not only do XPASS but also focus on smart
ones for XFAIL."

The hole this closes: the suite proved what OAAS accepts, nothing about what
it refuses — a parser accepting everything would have passed every positive
fixture. Now:
- TAXONOMY (spec/conformance.md §2): temporal pins (EXPECTED-FAIL: GAP-n,
  corpus/, closable via XPASS ritual) vs permanent rejections (MUST-FAIL:,
  conformance/rejections/, never flipped — XPASS there is always regression).
- GAP-2 PINNED (corpus 021): the transcript's ORIGINAL colon-binding form
  (`privacy: local_only`). Smart property: falsifiable in both directions —
  ratify colon-bindings and it XPASSes into the ritual; ratify =-only and
  the fixture is deleted in the same change.
- FOUR REJECTIONS, one per normative refusal: R001/R002 kind-purity both ways
  (ADR-0005 — the policy-inside-the-language boundary now has proof, not just
  claim); R003 adjacency (G2/ex-GAP-3); R004 output arity (G6).
- Selection principle recorded: one fixture per rule, anchored to its ADR;
  random syntax errors are noise, not rejections.
Validator: rejections scanned, marker-required (BADMETA otherwise).
State: 22 corpus + 4 rejection fixtures; 60/60; 38/38; just test green.
