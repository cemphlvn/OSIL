# G13 report — 2026-08-12
Gate: stratification closure — the missing middle defined, resolved, and
legality-checked. RESULT: PASS (all 8 gate items below).
Ratification: maintainer design review (Option A adopted: core-defined
strata; realization tightened per maintainer's definition; legality folded
into G13 rather than a separate G14).

## Sequence — the maintainer's 8-point gate, item by item
1. OAAS-SIR univocally defined (core.md, Aristotelian). ✓
2. OAAS-CIR univocally defined. ✓ (+ OAAS-NATIVE — a THIRD undefined stratum
   token found in toolchain.oaas's Identity projection during implementation;
   the core universe is {SIR, CIR, NATIVE}.)
3. realization univocally defined — by what it preserves and satisfies,
   never by endpoints; one-to-many; the constitutional equation lands:
   semantic optimization space = valid realizations(SIR, constraints,
   invariants). ✓
4. Every projection.from resolves: 5/5 sources (004 x3, CONTRACT.oaas,
   Identity). ✓
5. Source-stratum legality checked, derived from preserved dimension:
   EGraph<-SIR ✓ · ONNX<-CIR ✓✓ · MLIR<-CIR ✓ · Identity<-NATIVE ✓.
6. Unknown stratum MUST fail: RS005 (MagicIR) REJECTs. ✓ And the checks do
   NOT collapse: RS006 (EGraphWrong from OAAS-CIR preserve equivalence)
   RESOLVES then fails LEGALITY — the maintainer's negative test, verbatim. ✓
7. Existing corpus parses unchanged: 22 fixtures untouched, all PASS. ✓
8. Grammar productions added = 0 — inventory 61 == 61, mechanically shown.
   "Pause horizontal growth" held as a gate invariant, not advice. ✓

Also: GOVERNANCE.md gains the witness-diversity requirement (foreign-reader /
alternate-model lane in self-audits) — the external-witness observation made
structural, per maintainer direction.

## Final state
Resolution rate 18/18 = 1.00 · stratum sources 5/5, legality clean · 6
resolution refusals (RS001-006) · grammar v0.5 unchanged, 61 productions ·
just test green.

## Honesty notes
- Option A means strata live in the resolver's core table and core.md prose —
  NOT in any parseable .oaas artifact. The meta/object boundary is the point,
  but it leaves the strata outside conformance test #0's reach by design;
  if that ever feels wrong, design B (reification) is the recorded alternative.
- The legality map (dimension -> stratum) currently has four entries and no
  extension mechanism; "profile-defined compatible strata" is specified as a
  future allowance, not implemented.
- The stratification is defined but nothing yet COMPUTES over it: realization
  sets are vocabulary, not machinery. That is the e-graph/wizard/compiler
  track's job, exactly as the dependency chain orders it.
