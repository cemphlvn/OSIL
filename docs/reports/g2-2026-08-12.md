# G2 report — 2026-08-12
Gate: the repo's own operating policy is expressible in OAAS (conformance test #0).
RESULT: PASS. Ratification: explicit maintainer instruction ("continue to G2 and
close GAP-1") — grammar changes are constitutional-grade per GOVERNANCE.md.

## The closure sequence, as evidence that the guard works
1. Grammar v0.1 -> v0.2: +8 productions (actor_decl, actor_field, scope_block,
   verbs_block, invariants_block, ratify_block, path_list, path_ref); parser
   extended in lockstep; corpus 010-actor-policy.oaas added (exercises all four
   actor field kinds, multi-component paths, trailing slashes).
2. `just check` BEFORE marker removal: **XPASS fired, build red** — 
   "expected to fail (a GAP closed without ratification?)". The guard held.
3. Marker flip attempted; build STAYED red: the new header's historical prose
   ("carried EXPECTED-FAIL from G0") re-triggered the crude substring detector.
   Lesson banked: **marker semantics must be syntactic, not substring** — the
   detector now requires `// EXPECTED-FAIL:` at line start (matching the
   convention corpus-gardener already documented).
4. Final state: 12/12 files PASS (repo-policy.oaas among them), inventory 50==50
   [match], production coverage 50/50, alternative coverage 27/34 (same 7
   wishlist gaps as G1 — actor alternatives all covered by 010).

## Rode along, explicitly ratified in the same change
- GAP-3 closed: juxtaposition = ADJACENCY (no whitespace) for quantity
  number+unit — the same rule that makes multi-path scope blocks parseable
  (`profiles/ecosystem/onnx/ conformance/matrix/`). One rule, two gaps.
- GAP-2 remains open (`:` vs `=` binding univocity) — univocity-lint's queue.

## What G2 means
The policy layer is now inside the language: actors, scopes, verbs, invariants,
ratification points are parseable vocabulary. Next enforcement rung (pre-G3
option): a check that improvable/*/SKILL.md frontmatter agrees with the parsed
repo-policy.oaas actors — the agreement loop GOVERNANCE.md promises, now
mechanically possible because both sides are machine-readable.
