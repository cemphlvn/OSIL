# G4 report — 2026-08-12 (self-description bootstrap P0-P3)
Gate: golden-render diff = 0 across round-trip. RESULT: PASS.
Plan executed: docs/design/self-description-bootstrap.md. D1-D3 adopted under
delegated judgment; canonical open discussions: conformance/golden-render/README.md.

- P0/P1 (grammar v0.2 UNCHANGED): domain.toolchain vocabulary + G1/G3 pipelines
  as flows. Type production generalized for free (Files<oaas>[N]). Alternative
  coverage 34/34. GAP-4 (multi-output edges) discovered and pinned (018).
- P2 (grammar v0.3): layout block — 9 productions (in-document, ID-anchored,
  same artifact class). Inventory 59==59; coverage 59/59; alts 36/36.
- P3: tools/render_check.py — data gate / SVG advisory (deterministic by
  construction: fixed-metrics monospace, no font measurement) / pixels never;
  anchoring validation (D2); identity round-trip zero-diff. First golden:
  019 = OAAS's own render pipeline diagram (conformance test #0 extended to
  the visual dimension, "test #0v"). Blessed, then verified PASS.
- Use case #2 born: OAAS describes engineering loops, not just compute graphs.
