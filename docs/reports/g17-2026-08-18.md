# G17 report — 2026-08-18
Gate: the rename. OSIL everywhere living, history frozen, suite green under
the new name. RESULT: PASS.
Ratification: maintainer decision dialog (name: OSIL; depth: full).

## Sequence
1. Candidates screened live against GitHub (ontoagentics clean, ontoflow
   taken, osil minor noise, realis crowded, semport weak); maintainer chose
   OSIL, the descriptive-acronym convention, echoing the OSI layer family.
2. GitHub repo renamed oaas -> osil (old URL redirects); description updated;
   local remote repointed.
3. Deep sweep: 34 .oaas -> .osil (git mv, ids stable), 3 tools + grammar file
   renamed, strata tokens OAAS-* -> OSIL-* through corpus, contracts, spec,
   resolver, and view goldens; 64 living files swept, longest tokens first;
   docs/ excluded as frozen history (ADR-0012 records the one normalization
   caveat: in-fixture provenance path strings).
4. just test on first post-sweep run: ALL GREEN — check, resolve, roundtrip,
   egraph, render, policy, stages (28/28, ES004 pin holds), views (witnesses
   2/2). Zero grammar growth.
5. Backwards naming prose repaired by hand (the sweep had made "OAAS is a
   working name" into "OSIL is a working name", which inverted the meaning);
   README title now carries the expansion; CONTRIBUTING and GOVERNANCE naming
   notes resolved; GATES.md header note added.

## Final state
Repo: github.com/cemphlvn/osil (public, Apache-2.0) · suite green · GX has
NO remaining maintainer blockers: license ratified, name resolved.

## Honesty notes
- A mechanical sweep preserves tokens, not meaning: two prose sections came
  out asserting the opposite of the truth and were caught by reading, not by
  any harness. Prose has no XPASS guard; witness diversity applies to renames
  too.
- In-fixture provenance strings now cite .osil paths for events that happened
  under .oaas names; git history preserves the originals (ADR-0012).
- The user-side working directory is still named oaas; renaming it is the
  user's call (session memory is keyed to the directory path).
