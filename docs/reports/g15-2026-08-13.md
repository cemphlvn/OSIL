# G15 report — 2026-08-13
Gate: architecture as test object — a stage-composition term language (one
new construct, triple representation) with commutation guarded by write-set
disjointness; the repo's own pipeline is the suite: render/policy must
derive as commuting, roundtrip/egraph must be pinned non-commuting.
RESULT: PASS (pair matrix 15/15 as declared; pin holds).
Ratification: gate staged by maintainer instruction 2026-08-12 ("Put G15
before GX"), started by "Kesin — start G15"; the staged row authorizes the
grammar enlargement; construct design recorded in ADR-0010.

## Sequence
1. Recon fixed three design constraints: `.` is taken (namespace paths —
   overloading = the ADR-0008 sin), `render` writes only under `--bless`
   (normal run writes nothing), and the REAL collision is file-level
   (matrix.yaml). Composition therefore became the CONTEXTUAL keyword
   `then` — no tokenizer change, univocal.
2. ADR-0010: generic-rule-in-corpus (universals in vocabulary, instances
   derived), file-granular write-sets, computed guard, suite separation by
   `// SUITE:` directive, Bernstein read-write refinement recorded as the
   rejected-for-now tightening.
3. Triple shipped in one change (commit 2aeef75): grammar v0.6 (+3
   productions: stage_decl, resource_block, compose_expr — inventory
   61 -> 64, [match]); corpus 023 (the `just test` pipeline declared) and
   024 (generic `a then b <=> b then a` under writes_disjoint); boundary
   obligation R007 (stage-in-flow REJECTs — document-kind family) and R008
   (compose mixed with arithmetic REJECTs — sort boundary); core.md +
   TERMS.md definitions (stage, then, writes_disjoint — Aristotelian).
   First full run: 64/64 productions, 44/44 alternatives.
4. Machinery (commit 1330a4a): tools/stage_commute.py computes
   writes_disjoint(A,B) from DECLARATIONS (first non-hand-asserted guard;
   both orders registered — symmetry asserted, not assumed), registers the
   generic rule with the binary relation as condition, and gates the FULL
   pairwise matrix — both directions of expectation: disjoint pairs MUST
   derive, colliding pairs MUST be withheld.
5. Run: 15/15 pairs as declared. `render then policy` COMMUTE (the gate's
   named positive). `egraph then roundtrip` WITHHELD on
   conformance.matrix.matrix_yaml — the G14 honesty-note wart is now a
   NORMATIVE non-equivalence. ES004 pin XFAIL-HOLDS with XPASS-alarm
   semantics: removing the collision later must trip ratification.
6. `just test` = check + roundtrip + egraph + render + policy + resolve +
   stages, exit 0 end-to-end.

## Final state
grammar v0.6, 64/64 productions fired, 44/44 alternatives (`just check`) ·
corpus 24 fixtures (+023, 024) · rejections R001–R008 (+2) · stage pair
matrix 15/15 as declared, pins 1/1 hold (`just stages`) · EGraph
preservation 4/4 + adapter self-suite 3/3 unchanged (`just egraph`) ·
resolution 18/18 (`just resolve`) · full gatekeeper green (`just test`).

## Honesty notes
- **Declared-vs-actual write-sets**: corpus 023 declares what the tools
  write; nothing yet extracts write-sets from tool source. A tool gaining
  an undeclared write would not be caught — the declarations carry
  self-class trust, and mechanical extraction is named future work
  (ADR-0010), not assumed.
- **The guard is write-write only.** The Bernstein read-write refinement
  would ALSO pin egraph-after-roundtrip ordering (egraph reads matrix_yaml
  to preserve foreign cells) — a real dependency the current guard does not
  see. Recorded, unimplemented, separately ratifiable.
- **Generic-rule variable capture is silent**: in corpus 024, an identifier
  that names no declared stage becomes a pattern variable without warning —
  a typo'd stage name in a generic rule would quietly generalize. Pins
  reject undeclared stages loudly; the generic form does not. Follow-up
  candidate for univocity-lint.
- **v0 guard mapping is special-cased**: `writes_disjoint = true` binds the
  rule's two pattern variables in LHS order; n-ary compositions and other
  computed guards have no mapping yet.
- **Witness diversity (GOVERNANCE SHOULD)**: same model family built and
  checked everything; the foreign-witness lane is the maintainer reading
  this report. The pin/alarm structure is the mechanical hedge: the load-
  bearing claims fail loudly rather than depending on reviewer attention.
