# G10 report — 2026-08-12
Gate: the ritual system is complete — both frontiers governed, format owned.
RESULT: PASS.
Ratification: maintainer instruction ("apply both as G10, partition A/B,
start with G10-B"; reporter directive in the same instruction).

## Sequence
1. G10-B first (norm before instance): spec/conformance.md §2 — pin lifecycle
   gains the PROMOTE exit (pin -> rejection; anticipated for 021/GAP-2 under
   an =-only ratification); BOUNDARY OBLIGATION (an enlarging ritual ships
   its refusals or declares none, justified); ritual-XPASS vs alarm-XPASS.
   GOVERNANCE.md merge gate item 4. Commit 1f7477d.
2. G10-A: R005-dangling-dot.oaas discharges the boundary G9 left owed
   (refusal enforced in the parser, unproven in conformance until now).
   REJECT on first run. Commit c4789e7.
3. Reporter: improvable/gate-reporter/ — the report format extracted from
   g1–g9 into an explicit template (RESULT-first, sequence-as-proof,
   moved-counters-only, honesty notes REQUIRED). Routed in INDEX + CLAUDE.md
   + AGENTS.md; policy actor added (repo-policy v3) because policy_check
   demands 1:1 — the G8 loop constrained its own author.

## Final state
23 corpus + 5 rejection fixtures · 61/61 productions · 40/40 alternatives ·
policy agreement 9 actors / 8 skills (1 human-exempt) · just test green.

## Honesty notes
- The boundary obligation is retroactively self-indicting: G6 and G9 both
  violated it before it was named; R004 and R005 discharged that debt. The
  norm exists because we broke it twice, not because we foresaw it.
- This report is the template's first instance — the template is v1 opinion;
  gate-reporter's evals are empty at bootstrap and grow from failure signals
  per skill-improver discipline.
- G10 enlarged no grammar, so its own boundary obligation is: no new
  boundary — justified by construction (spec text, one rejection fixture,
  one skill; grammar untouched at v0.5).
