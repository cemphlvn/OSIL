# G8 report — 2026-08-12 (structure-improvement session, contemporary grammar)
Gate: the tree's structure is self-checking under grammar v0.4. RESULT: PASS.

S1 — POLICY AGREEMENT LOOP (headline): repo-policy.oaas extended to cover
every deployed skill 1:1 (8 actors, spec-editor human-exempt);
tools/policy_check.py parses actors OUT OF THE OAAS TEXT and verifies
frontmatter agreement (1:1, scope cover, verb equality). Wired into
`just test` and CI. The loop GOVERNANCE.md promised at G0 is mechanical.
First run green — direction-of-fit noted (both sides authored together);
value = catching future drift.
S1-finding — GAP-5 discovered by self-description pressure: path_ref cannot
express file-granular scopes (spec/TERMS.md). Pinned by corpus 022
(EXPECTED-FAIL); scopes directory-granular until ratified closure.
S2 — definition debt cleared: core.md gains actor / regime / rejection
fixture (Aristotelian); chapter map refreshed (conformance.md, versioning.md,
TERMS.md now listed).
S3 — ecosystem triad linked: registry entry now indexes profile + contract +
suite (fission of an adapter = follow the registry entry).
S4 — curriculum measurable: minimal-spine.yaml GENERATED from the covering
set; resolution + reachability in every scan (currently 8 unreachable items,
report-only — pedagogy stays ratified).
S5 — CI (.github/workflows/gates.yml): corpus contract, policy agreement,
golden render, round-trip, compression — the merge gate stops being
aspirational on push.
Baseline loop validated in passing: passthrough ratios rose (matmul
2.43->2.67) — explained by G6's node-proto strengthening; the scout's
explained-vs-silent distinction did its job.
