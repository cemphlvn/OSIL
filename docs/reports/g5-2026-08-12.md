# G5 report — 2026-08-12
Gate: vocabulary self-extends from detector findings. RESULT: PASS.
The full loop closed, each step an artifact: detector finding
(compression-2026-08-12.md, name direction) -> formal proposal (ADR-0007,
PROPOSED) -> maintainer ratification -> vocabulary landed
(profiles/domain/numeric/numeric.oaas) -> canonical-form fixture (020,
associativity under regime = ExactArithmetic) -> synonym pair recorded
(spec/TERMS.md first entry) -> re-baseline (20 fixtures; coverage 59/59,
alts 36/36 — the named form exercises no new productions BY DESIGN: the whole
point was expressibility within the existing grammar).

Honesty note on "resolves the naming candidate": the raw pattern
`guards { numeric_semantics = ... }` STILL appears in the scan, because
fixtures 003/009/013/014/015 retain it transcript-faithfully as the declared
expansion. Resolution = the pattern now HAS a name, a canonical form, and a
TERMS entry — not that it vanished. The scout's future runs classify it as
"named, legacy form retained" per its step-4 procedure.
