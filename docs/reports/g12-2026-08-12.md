# G12 report — 2026-08-12
Gate: the resolver — every reference finds its universal. RESULT: PASS.
Ratification: maintainer instruction ("close the resolver as G12", with
grounding + perspective-accumulation mandated first).

## Sequence
1. GROUNDING measured, not assumed: `use ecosystem.onnx` was DANGLING (no
   in-language profile existed — pins lived in plain-text VERSIONS); 9/9
   toolchain flow-ops undeclared; registry oracle empty. The gate's work was
   discovered by the gate's own lens.
2. Perspectives synthesized (g12-resolver-plan.md): linker (two-pass,
   suggestions), LSP (shared reader — no fifth reader minted: oaas_read.py),
   ontology (resolution = particular finds universal), security (namespace
   collision = dependency confusion), ONNX precedent (rung 3 excluded).
3. Vocabulary completed under resolution pressure: profile.oaas born as
   CANONICAL pins (VERSIONS mirrors, resolver enforces agreement); 9 operator
   declarations added to toolchain.oaas; registry operators oracle populated.
4. tools/oaas_resolve.py: rung 1 (use->profile, terminal-segment namespace
   binding, collision detection, dataflow wiring) + rung 2 (registry oracle
   for ecosystems, directory vocabulary for domains, pin consistency).
5. Boundary obligation applied VOLUNTARILY to a new acceptance layer:
   conformance/resolution/ RS001-RS004 (dangling use, unbound namespace,
   undeclared op, broken wiring) — parse fine, REJECT at resolution.
6. First full run: RESOLVE 6/6 flows, rate 18/18 = 1.00, PINS-OK, 4/4
   refusals REJECT. Wired into `just test` + CI.

## Final state
Resolution rate 1.00 (north-star #1: was ~0) · 6 flows resolve · 4 resolution
refusals · universe: 4 profiles, 4 namespaces, 11 operators + 3 concepts ·
readers: shared module born (metric #2 stops growing) · just test green.

## Honesty notes
- Rate 1.00 is over OUR six flows against OUR universe — the direction-of-fit
  caveat applies as always; RS001-004 are the counterweight.
- Unused-value infos (Y in 002, text/passthrough in 018, svg in 019) are
  real observations the resolver surfaced — left as info, not errors, since
  fixtures legitimately illustrate fragments.
- oaas_read.py prevents a FIFTH reader; the four existing ones still stand —
  metric #2 improves only when migration happens, and this gate did not do it.
