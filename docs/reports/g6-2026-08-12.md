# G6 report — 2026-08-12
Gate: GAP-4 (multi-output edges) closed with teeth. RESULT: PASS.
Ratification: maintainer instruction ("close GAP-4 as G6").

Sequence (the XPASS ritual, second performance):
1. Grammar v0.3 -> v0.4: edge_stmt gains out_spec; `-> (Y, Z)` ratified as
   POSITIONAL multi-output (D3). Parser in lockstep (+1 production, +2 alts).
2. `just check` BEFORE marker flip: XPASS fired on 018, build red — guard held.
3. Marker flipped in the ratified change; 018 is now a normal fixture
   exercising out_spec:multi. Ledger: GAP-4 CLOSED in grammar/GAPS.md.
4. Teeth (D3's own revisit trigger): ONNX Split case added — first
   multi-output operator through the harness. export_flow emits `-> (Y1, Y2)`;
   full NodeProtos ride the sanctioned passthrough (carrying the axis
   attribute), with text-authoritative cross-checks on op/inputs/outputs.
   Preservation score 4/4 over {add, matmul, split}.
5. render_check reader handles multi-dst edges; D2 anchoring unchanged (a
   layout edge references one (src, dst) pair; a multi-output edge simply
   admits several such pairs).
Final: 21 fixtures; inventory 60==60; coverage 60/60; alternatives 38/38;
just test fully green.
