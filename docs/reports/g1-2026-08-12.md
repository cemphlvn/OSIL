# G1 report — 2026-08-12
Gate: 100% corpus parses; every production exemplified. RESULT: PASS.
- `just check`: 9/9 corpus files (8 .oaas + 1 .flow), 42/42 productions fired.
- profiles/ also validated: onnx/CONTRACT.oaas PASS; repo-policy.oaas XFAIL (as
  designed — conformance test #0 stays red until G2; XPASS now fails the build).
- Corpus delta at this gate: 002 renamed .oaas->.flow + `use` header (ADR-0005);
  009 strength-reduction equivalence added (transcript section 2).
- Gap discovered mechanically on first run: GAP-3, quantity lexing boundary
  (0.997\nmemory lexed as quantity). v0 parser rule: same-line juxtaposition;
  ratified grammar text still owed.
- Enablement analysis: docs/design/g1-enablement.md.
