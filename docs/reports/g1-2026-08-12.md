# G1 report — 2026-08-12
Gate: 100% corpus parses; every production exemplified. RESULT: PASS.
- `just check`: 9/9 corpus files (8 .oaas + 1 .flow), 42/42 productions fired.
- profiles/ also validated: onnx/CONTRACT.oaas PASS; repo-policy.oaas XFAIL (as
  designed — conformance test #0 stays red until G2; XPASS now fails the build).
- Corpus delta at this gate: 002 renamed .oaas->.flow + `use` header (ADR-0005);
  009 strength-reduction equivalence added (transcript section 2).
- Gap discovered mechanically on first run: GAP-3, quantity lexing boundary
  (0.997\nmemory lexed as quantity). v0 parser rule: same-line juxtaposition;
  ratified grammar text still owed.
- Enablement analysis: docs/design/g1-enablement.md.

## Addendum (same day): how is 42/42 possible? — user challenge, answered mechanically
The perfect score was legitimate but weaker than it looked, for three reasons:
1. DIRECTION OF FIT: the grammar was reverse-engineered FROM the corpus (both
   derive from the transcript's DSL blocks), so corpus-covers-grammar is close to
   true by construction. The one hole that existed (mul_op `<<`) is exactly why
   fixture 009 was added at G1.
2. SELF-ASSERTED DENOMINATOR (fixed): "42" was a hand-written list inside the
   validator. Now derived from oaas.ebnf itself; any grammar<->validator
   inventory mismatch fails the build. Current: 42 declared == 42 instrumented.
3. GRANULARITY (now reported): the gate counts PRODUCTIONS, not ALTERNATIVES.
   Alternative-level coverage is 23/30; the 7 gaps (informational, corpus
   wishlist): add_op:-, io_decl:output, literal:string, mul_op:/, mul_op:>>,
   rel_op:==, rel_op:>. These are honest holes: no corpus example subtracts,
   divides, right-shifts, declares an output, or uses a string literal value yet.
