# Eval fixtures

Each fixture: a markdown file stating SITUATION (what the runner faces) and
EXPECTED BEHAVIOR CLASS (what a correct execution of the skill produces).
Fixtures marked `held-out: true` are for measuring, not optimizing — the
skill-improver must not read them while editing bodies. Grow fixtures from real
failure signals (see skill-improver step 5).
