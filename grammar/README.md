# grammar/ — subtree card
ground-truth: SELF · CRITICAL PATH: no engineering loop anywhere can bite until this parses the corpus
cadence: slow (every change triggers full-corpus re-validation)
version-stream: grammar (v0 draft)
loops: corpus parse-check · production-coverage (every production has >=1 corpus example)
invariants: corpus-compatibility (a grammar change may not orphan corpus files without ratified migration)
policy: propose-only; grammar changes are constitutional-grade
gaps: see GAPS.md — GAP-1 is deliberate (conformance test #0, gate G2)
