# curriculum/ — subtree card
ground-truth: SELF (pedagogy) over SHARED content (the corpus)
cadence: continuous
loops: reachability (every corpus item reachable from >=1 path) · resolution (every step's corpus id exists)
invariants: PATHS ARE VIEWS — ids referencing conformance/corpus/ only; NEVER duplicated content
policy: agents author paths freely; pedagogy ordering ratified by humans
heritage: this subtree is why the project exists — it began as a learning decision (an MSc), and the open resource replaces the credential
