# resolution/ — what RESOLUTION refuses (subtree card)

The boundary obligation (G10-B) applied voluntarily to a new acceptance
layer: these fixtures PARSE fine but must fail RESOLUTION. Marker:
`// MUST-FAIL-RESOLUTION: <rule>` — never flipped; a clean resolve here is a
regression (alarm), never a ritual. One fixture per refusal:
- RS001 dangling use · RS002 unbound namespace · RS003 undeclared op ·
  RS004 broken wiring
Scanned by tools/oaas_resolve.py only (parse-level rejections live in
conformance/rejections/).
invariants: marker-required · never-flipped · one-fixture-per-rule
