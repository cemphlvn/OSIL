# golden-render/ — deterministic render-diff fixtures (gate G4)
Empty until tools/ hosts a deterministic renderer and research U4 fixes the diff
strategy (structural vs pixel). Goldens update only via human-ratified PR.
