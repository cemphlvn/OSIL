# Interchange Format
Status: stub (draft-0). Will define: the OAAS native serialization — which is the
IDENTITY PROJECTION (total preservation contract, empty may_lose; includes
visual_layout). Round-trip rule: unknown fields survive as opaque namespaced
annotations. Blocked behind: G1; layout schema input from research U4.
