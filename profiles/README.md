# profiles/ — subtree card
Three ground-truth classes live under this directory — check the child's card:
- ecosystem/  FOREIGN   (ONNX, egg, MLIR, WASM own their semantics; sync+report only)
- ontology/   ACADEMIC  (BFO, DOLCE, UFO publications; cite + audit fidelity)
- domain/     SELF      (ml, crypto, agent; agent/ hosts the self-hosted repo policy = conformance test #0)
