// machine-readable preservation contract for the ONNX projection
// provenance: intake transcript "ONNX projection" checklist (2026-08-12)
// verification status: all four `preserves` fields VERIFIED mechanically at G3
// (2026-08-12, preservation score 4/4 over cases {add, matmul} — scope note:
// suite is v0-narrow; see docs/reports/roundtrip-onnx-2026-08-12.md)
projection ONNX {
    from OSIL-CIR
    preserve computation
}

preserves {
    tensor_types
    operator_versions
    graph_topology
    constants
}

may_lose {
    ontology_annotations
    visual_layout
}
