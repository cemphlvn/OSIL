# profiles/domain/numeric/ — subtree card
ground-truth: SELF · cadence: with spec · born: ADR-0007 (G5, 2026-08-12)
Numeric regimes as concepts. Canonical guard form: `regime = <Concept>`;
raw form `numeric_semantics = <value>` is its declared expansion (synonym pair
tracked in spec/TERMS.md). Carrier lists are ILLUSTRATIVE until the egg
adapter lands (U5) — curation happens there.
loops: univocity (synonym tracking) · corpus refs resolve · compression re-baseline
policy: vocabulary additions propose-only (detector -> ADR -> ratification)
