# validates against registry/schema/manifest.schema.json (v0)
name: onnx
kind: ir
capabilities: [graph-interchange, versioned-operators, runtime-execution-via-ort]
operators: [MatMul@13, Add@13, Split@13]   # the oracle (G12): ops the suite covers
requirements: []
versions:
  ir_version: 11        # ASSUMPTION: unverified, mirrors profiles/ecosystem/onnx/VERSIONS
  opset.ai.onnx: 24     # ASSUMPTION: unverified
targets: [cpu, gpu, coreml, wasm]
cost_hints: {}
recipes: []
interop:                    # REGISTERED (G3): described -> tested ecosystem
  suite: conformance/interop/onnx
  contract: profiles/ecosystem/onnx/CONTRACT.osil
  profile: profiles/ecosystem/onnx        # triad linked (G8): the registry
                                          # entry is the ecosystem's index
